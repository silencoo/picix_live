# 快速开始指南

## 📋 文件说明

- `picix_bot/` - Telegram Bot 程序包
- `auto_unlock_helper.py` - Picix API 与命令行助手
- `pyproject.toml` / `uv.lock` - uv 项目依赖与精确锁文件
- `start.bat` - Windows交互式控制中心
- `bot.bat` - 快速启动 Telegram Bot
- `unlock.bat` - 快速执行每日解锁
- `status.bat` - 快速查看状态
- `start.sh` - Linux/macOS交互式控制中心
- `bot.sh` / `unlock.sh` / `status.sh` - Linux/macOS快捷入口
- `README_助手.md` - 详细使用说明
- `unlock_data/` - 数据目录（自动创建，包含解锁记录）

## 🚀 三步开始

### 1. 安装并同步 uv 环境

```powershell
winget install --id=astral-sh.uv -e
uv sync --locked
```

### 2. 测试运行

双击 `start.bat` 后选择“Show Picix Status”，或运行：
```powershell
uv run --locked --env-file .env python auto_unlock_helper.py status
```

Linux/macOS 也可以运行 `./start.sh status`。

### 3. 执行每日解锁

双击 `start.bat` 后选择“Run Daily Unlock”，或运行：
```powershell
uv run --locked --env-file .env python auto_unlock_helper.py unlock
```

Linux/macOS 也可以运行 `./start.sh unlock`。

## 📅 日常使用

**每天执行一次**：
- 双击 `unlock.bat`
- 或运行 `uv run --locked --env-file .env python auto_unlock_helper.py unlock`

**查看进度**：
- 双击 `status.bat`
- 或运行 `uv run --locked --env-file .env python auto_unlock_helper.py status`

## ⚙️ 设置定时任务（可选）

### Windows 任务计划程序

1. 打开"任务计划程序"
2. 创建基本任务
3. 触发器：每天（建议晚上）
4. 操作：启动程序
   - 程序：`uv`
   - 参数：`run --locked --env-file .env python auto_unlock_helper.py unlock`
   - 起始于：项目根目录

## 💡 策略提醒

- ✅ 每月记得购买2次「资源轻量包」（450分 × 2）
- ✅ 脚本会自动优先从片单选择，确保满足片单任务
- ✅ 如果某天忘记执行，可以在后续日期补上
- ✅ 只要月内达到50个解锁即可完成所有任务

## 📊 预期收益

- **支出**: 900分（2次轻量包）
- **收入**: 1190分
  - 每日任务：450分（15 × 30天）
  - 月常任务：640分（50个）
  - 片单任务：100分（20个）
- **净收益**: +290分/月

## ❓ 常见问题

**Q: 脚本找不到可解锁的电影？**
A: 可能是所有片单和推荐列表的电影都已解锁，可以手动选择其他电影解锁。

**Q: 如何查看详细的解锁记录？**
A: 查看 `unlock_data/unlock_log.json` 文件。

**Q: 可以修改片单选择策略吗？**
A: 可以编辑 `auto_unlock_helper.py` 中的 `daily_unlock()` 函数。

## 🔧 故障排除

如果遇到问题：
1. 检查网络连接
2. 确认 authorization 是否有效；失效时通过 Bot 的 `/reauth` 完成续期
3. 查看脚本输出的错误信息
4. 检查 `unlock_data/` 目录权限
