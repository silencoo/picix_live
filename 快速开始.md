# 快速开始指南

## 📋 文件说明

- `picix_bot/api/` - Picix HTTP 与认证层
- `picix_bot/services/` - 商城、任务、资源包与解锁服务
- `picix_bot/cli.py` - `picix` 统一命令入口
- `auto_unlock_helper.py` - 旧脚本兼容层
- `pyproject.toml` / `uv.lock` - uv 项目依赖与精确锁文件
- `start.bat` - Windows交互式控制中心
- `bot.bat` - 快速启动 Telegram Bot
- `unlock.bat` - 快速执行每日解锁
- `status.bat` - 快速查看状态
- `start.sh` - Linux/macOS交互式控制中心
- `bot.sh` / `unlock.sh` / `status.sh` - Linux/macOS快捷入口
- `README_助手.md` - 详细使用说明
- `unlock_data/` - 数据目录（自动创建，包含解锁记录）

## 🚀 三步开始

### 1. 安装并同步 uv 环境

```powershell
winget install --id=astral-sh.uv -e
uv sync --locked
```

### 2. 测试运行

双击 `start.bat` 后选择“Show Picix Status”，或运行：
```powershell
uv run --locked --env-file .env picix status
```

Linux/macOS 也可以运行 `./start.sh status`。

### 3. 启动自动优化

双击 `start.bat` 后选择“Run Points Optimization”，或先只查看计划：
```powershell
uv run --locked --env-file .env picix plan
uv run --locked --env-file .env picix optimize
```

Linux/macOS 也可以运行 `./start.sh plan` 或 `./start.sh optimize`。

## 📅 日常使用

**推荐常驻 Bot**：
- 双击 `bot.bat`
- 或运行 `./start.sh bot`
- Bot 会每天自动完成日更、补量和按需购包

**查看进度**：
- 双击 `status.bat`
- 或运行 `uv run --locked --env-file .env picix status`

## ⚙️ 设置定时任务（可选）

### Windows 任务计划程序

1. 打开"任务计划程序"
2. 创建基本任务
3. 触发器：每天（建议晚上）
4. 操作：启动程序
   - 程序：`uv`
   - 参数：`run --locked --env-file .env picix unlock`
   - 起始于：项目根目录

## 💡 策略提醒

- ✅ 不再固定每月买两包，优先复用已有有效额度
- ✅ 额度不足或30天周期低消未达450分时才按需购包
- ✅ 脚本会自动优先从片单选择，确保满足片单任务
- ✅ 根据任务真实截止时间自动计算并执行补量

## 📊 收益说明

固定两包方案单周期账面净收益是290分，但会留下10次额度。新策略让这些
额度跨周期继续使用，之后仅在真正缺额度时购包，因此长期平均收益更高。
滚动30天周期消费不足450分时，用一个轻量包替代无收益的低消扣减。

## ❓ 常见问题

**Q: 脚本找不到可解锁的电影？**
A: 可能是所有片单和推荐列表的电影都已解锁，可以手动选择其他电影解锁。

**Q: 如何查看详细的解锁记录？**
A: 查看 `unlock_data/unlock_log.json` 文件。

**Q: 可以修改片单选择策略吗？**
A: 解锁流程位于 `picix_bot/services/automation.py`；选片与单次解锁位于
`picix_bot/services/unlock.py`。

## 🔧 故障排除

如果遇到问题：
1. 检查网络连接
2. 确认 authorization 是否有效；失效时通过 Bot 的 `/reauth` 完成续期
3. 查看脚本输出的错误信息
4. 检查 `unlock_data/` 目录权限
